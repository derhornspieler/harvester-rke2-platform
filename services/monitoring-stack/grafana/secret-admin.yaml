apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: monitoring
  labels:
    app: grafana
type: Opaque
stringData:
  # CHANGEME: Set a strong password before deploying
  admin-password: "CHANGEME_GRAFANA_ADMIN_PASSWORD"
