apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-CHANGEME_DOMAIN_DASHED-tls
  namespace: kube-system
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  secretName: traefik-CHANGEME_DOMAIN_DASHED-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  dnsNames:
    - traefik.CHANGEME_DOMAIN
  duration: 720h
  renewBefore: 168h
