apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alertmanager
  namespace: monitoring
  labels:
    app: alertmanager
spec:
  parentRefs:
    - name: alertmanager
      namespace: monitoring
      sectionName: https
  hostnames:
    - "alertmanager.example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: keycloak-auth
      backendRefs:
        - name: alertmanager
          port: 9093
