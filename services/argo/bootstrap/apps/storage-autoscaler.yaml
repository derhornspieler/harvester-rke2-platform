apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: storage-autoscaler
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:derhornspieler/svc-CHANGEME_DOMAIN_DASHED-storage-autoscaler.git
    targetRevision: main
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: storage-autoscaler
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
