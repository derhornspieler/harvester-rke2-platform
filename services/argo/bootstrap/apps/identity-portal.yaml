apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: identity-portal
  namespace: argocd
spec:
  project: default
  source:
    repoURL: CHANGEME_GIT_BASE_URL/rke2-cluster.git
    targetRevision: main
    path: services/identity-portal
  destination:
    server: https://kubernetes.default.svc
    namespace: identity-portal
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
