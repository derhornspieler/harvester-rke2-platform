apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # TLS + Ingress (Gateway API + cert-manager gateway-shim)
  - gateway.yaml
  - httproute.yaml
