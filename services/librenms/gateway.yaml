apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: librenms
  namespace: librenms
  annotations:
    cert-manager.io/cluster-issuer: vault-issuer
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      protocol: HTTP
      port: 8000
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      protocol: HTTPS
      port: 8443
      hostname: librenms.CHANGEME_DOMAIN
      tls:
        mode: Terminate
        certificateRefs:
          - name: librenms-CHANGEME_DOMAIN_DASHED-tls
      allowedRoutes:
        namespaces:
          from: Same
