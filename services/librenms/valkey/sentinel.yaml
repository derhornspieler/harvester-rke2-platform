# OpsTree Redis Operator — RedisSentinel CRD for LibreNMS
#
# Naming: CRD name "librenms-redis" → operator creates:
#   StatefulSet: librenms-redis-sentinel   Pods: librenms-redis-sentinel-{0,1,2}
#   Service:     librenms-redis-sentinel   Headless: librenms-redis-sentinel-headless
# NOTE: Do NOT use kubernetesConfig.redisSecret on sentinel — it sets both
# requirepass (sentinel's own auth) AND masterauth. Harbor chart has no
# sentinelPassword field, so sentinel requirepass blocks harbor-core.
# Instead, use redisSentinelConfig.redisReplicationPassword which sets
# ONLY masterauth (sentinel can auth to master, but clients can query
# sentinel without auth). Acceptable: sentinel is ClusterIP-only.
# If Harbor adds sentinelPassword support, re-enable requirepass.
apiVersion: redis.redis.opstreelabs.in/v1beta2
kind: RedisSentinel
metadata:
  name: librenms-redis
  namespace: librenms
spec:
  clusterSize: 3
  kubernetesConfig:
    image: quay.io/opstree/redis-sentinel:v7.0.15
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 128Mi
  redisSentinelConfig:
    redisReplicationName: librenms-redis
    masterGroupName: mymaster
    redisPort: "6379"
    quorum: "2"
    redisReplicationPassword:
      secretKeyRef:
        name: librenms-valkey-credentials
        key: password
  nodeSelector:
    workload-type: database
  podSecurityContext:
    runAsUser: 999
    fsGroup: 999
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchLabels:
                app: librenms-redis-sentinel
            topologyKey: kubernetes.io/hostname
